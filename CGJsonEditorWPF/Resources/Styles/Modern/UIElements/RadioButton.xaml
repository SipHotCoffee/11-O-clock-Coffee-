<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <SolidColorBrush x:Key="RadioButton.Normal.Background" Color="{StaticResource Control2}"/>
    <SolidColorBrush x:Key="RadioButton.Normal.Border"     Color="{StaticResource Control4}"/>
    
    <SolidColorBrush x:Key="RadioButton.MouseOver.Background" Color="{StaticResource Control4}"/>
    <SolidColorBrush x:Key="RadioButton.MouseOver.Border"     Color="{StaticResource Control4}"/>

    <SolidColorBrush x:Key="RadioButton.Pressed.Background" Color="{StaticResource Control1}"/>
    <SolidColorBrush x:Key="RadioButton.Pressed.Border"     Color="{StaticResource Control4}"/>

    <SolidColorBrush x:Key="RadioButton.Checked.Background" Color="{StaticResource Accent4}"/>
    <SolidColorBrush x:Key="RadioButton.Checked.Border"     Color="{StaticResource Control4}"/>

    <SolidColorBrush x:Key="RadioButton.Disabled.Background" Color="{StaticResource Control2}"/>
    <SolidColorBrush x:Key="RadioButton.Disabled.Border"     Color="Transparent"/>

    <Style TargetType="{x:Type RadioButton}">
        <Setter Property="Background"  Value="{StaticResource RadioButton.Normal.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource RadioButton.Normal.Border}"/>

        <Setter Property="HorizontalAlignment" Value="Left"/>

        <Setter Property="Foreground" Value="{StaticResource Window.Active.Foreground}"/>

        <Setter Property="FontSize" Value="14"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0" HorizontalAlignment="Left" Width="20" Height="20">
                            <Ellipse Fill="{TemplateBinding Background}" 
                                     Stroke="{TemplateBinding BorderBrush}"/>

                            <Ellipse x:Name="MiddleEllipse"
                                     Fill="{StaticResource Window.Active.Background}"
                                     VerticalAlignment="Center" 
                                     HorizontalAlignment="Center" 
                                     Width="10" Height="10" Visibility="Collapsed"/>
                        </Grid>
                        <ContentPresenter Margin="5" Grid.Column="1" Content="{TemplateBinding Content}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"  Value="{StaticResource RadioButton.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource RadioButton.MouseOver.Border}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"  Value="{StaticResource RadioButton.Pressed.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource RadioButton.Pressed.Border}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="Background"  Value="{StaticResource RadioButton.Checked.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource RadioButton.Checked.Border}"/>
                            <Setter TargetName="MiddleEllipse" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background"  Value="{StaticResource RadioButton.Disabled.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource RadioButton.Disabled.Border}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>