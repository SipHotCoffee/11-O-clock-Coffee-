<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
        <ResourceDictionary Source="Button.xaml"/>
        <ResourceDictionary Source="Menu.xaml"/>
        <ResourceDictionary Source="TextBox.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <SolidColorBrush x:Key="DatePicker.Background"          Color="{StaticResource Control1}"/>
    <SolidColorBrush x:Key="DatePicker.Border"              Color="{StaticResource Control2}"/>

    <SolidColorBrush x:Key="DatePicker.Inactive.Background" Color="{StaticResource Control1}"/>
    <SolidColorBrush x:Key="DatePicker.Inactive.Border"     Color="{StaticResource Control2}"/>

    <Style x:Key="CalendarDayButtonStyle" TargetType="{x:Type CalendarDayButton}">
        <Setter Property="Background"      Value="{StaticResource Button.Normal.Background}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource Button.Normal.Border}"/>
        <Setter Property="Foreground"      Value="{StaticResource Window.Active.Foreground}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" 
                            Margin="2"
                            Padding="8"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="_contentPresenter"
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding Content}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter Property="Background"  Value="{StaticResource Button.Disabled.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Button.Disabled.Border}"/>
                            <Setter Property="Foreground"  Value="{StaticResource Window.Active.Foreground.Disabled}"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"  Value="{StaticResource Button.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Button.MouseOver.Border}"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"  Value="{StaticResource Button.Pressed.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Button.Pressed.Border}"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background"  Value="{StaticResource Button.Defaulted.Normal.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Button.Defaulted.Normal.Border}"/>
                            <Setter Property="Foreground"  Value="{StaticResource Window.Active.InvertedForeground}"/>
                            <!--<Setter Property="TextElement.Foreground" TargetName="_contentPresenter" Value="{StaticResource Window.Active.InvertedForeground}"/>-->
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background"  Value="{StaticResource Button.Defaulted.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Button.Defaulted.MouseOver.Border}"/>
                            <Setter Property="Foreground"  Value="{StaticResource Window.Active.InvertedForeground}"/>
                            <!--<Setter Property="TextElement.Foreground" TargetName="_contentPresenter" Value="{StaticResource Window.Active.InvertedForeground}"/>-->
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background"  Value="{StaticResource Button.Defaulted.Pressed.Background}"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Button.Defaulted.Pressed.Border}"/>
                            <Setter Property="Foreground"  Value="{StaticResource Window.Active.InvertedForeground}"/>
                            <!--<Setter Property="TextElement.Foreground" TargetName="_contentPresenter" Value="{StaticResource Window.Active.InvertedForeground}"/>-->
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CalendarItemStyle" TargetType="{x:Type CalendarItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource Window.Active.Foreground}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarItem}">
                    <Grid>
                        <!-- Custom layout for the calendar grid -->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header with navigation buttons -->
                        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
                            <Button x:Name="PART_PreviousButton" Focusable="False" FontFamily="{StaticResource SymbolFont}" Content="&#xf0b0;" Margin="5, 5, 2.5, 2.5"/>
                            <Button x:Name="PART_HeaderButton" Content="Month" Focusable="False" Margin="2.5, 5, 2.5, 2.5"/>
                            <Button x:Name="PART_NextButton" Focusable="False" FontFamily="{StaticResource SymbolFont}" Content="&#xf0af;" Margin="2.5, 5, 5, 2.5"/>
                        </StackPanel>

                        <Grid x:Name="PART_MonthView" Grid.Row="1" Margin="5, 2.5, 5, 5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                        </Grid>
                        <Grid x:Name="PART_YearView"
                              Visibility="Hidden"
                              Grid.Row="1"
                              HorizontalAlignment="Center"
                              Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Calendar}}}" Value="Year">
                            <Setter Property="Visibility"
                                    TargetName="PART_MonthView"
                                    Value="Hidden" />
                            <Setter Property="Visibility"
                                    TargetName="PART_YearView"
                                    Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DisplayMode, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Calendar}}}"
                                     Value="Decade">
                            <Setter Property="Visibility"
                                    TargetName="PART_MonthView"
                                    Value="Hidden" />
                            <Setter Property="Visibility"
                                    TargetName="PART_YearView"
                                    Value="Visible" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CalendarStyle" TargetType="{x:Type Calendar}">
        <Setter Property="Background" Value="{StaticResource Window.Active.Background}"/>
        <Setter Property="Foreground" Value="{StaticResource Window.Active.Foreground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Menu.Separator.Background}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource CalendarDayButtonStyle}"/>
        <Setter Property="CalendarItemStyle" Value="{StaticResource CalendarItemStyle}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Calendar}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <CalendarItem x:Name="PART_CalendarItem" Style="{TemplateBinding CalendarItemStyle}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type DatePicker}">
        <Setter Property="Background"  Value="{StaticResource DatePicker.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource DatePicker.Border}"/>
        <Setter Property="Foreground"  Value="{StaticResource Window.Active.Foreground}"/>
        <Setter Property="CalendarStyle" Value="{StaticResource CalendarStyle}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
        <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="4">
                        <Grid x:Name="PART_Root" 
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Foreground="{TemplateBinding Foreground}"
                                    Focusable="False"
                                    HorizontalAlignment="Left"
                                    Margin="3,0,3,0"
                                    Grid.Row="0"
                                    VerticalAlignment="Top" />
                            <DatePickerTextBox x:Name="PART_TextBox"
                                               Grid.Column="0"
                                               Grid.Row="0"
                                               Background="{StaticResource TextBox.Normal.Background}"
                                               BorderBrush="{StaticResource TextBox.Normal.Border}"
                                               BorderThickness="1"
                                               Foreground="{TemplateBinding Foreground}"
                                               Focusable="{TemplateBinding Focusable}"
                                               HorizontalContentAlignment="Stretch"
                                               VerticalContentAlignment="Stretch">
                                <DatePickerTextBox.Template>
                                     <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                                         <Grid SnapsToDevicePixels="True">
                                             <Grid.RowDefinitions>
                                                 <RowDefinition Height="*"/>
                                                 <RowDefinition Height="auto"/>
                                             </Grid.RowDefinitions>
         
                                             <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2" Padding="5, 5, 5, 8" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                                 <ScrollViewer x:Name="PART_ContentHost" Focusable="false" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                                             </Border>

                                             <Border x:Name="focusBar" 
                                                     CornerRadius="0, 0, 2, 2" 
                                                     BorderThickness="0" 
                                                     Background="{StaticResource TextBox.Focused.Bar}"
                                                     Visibility="Collapsed"
                                                     Height="2" 
                                                     Margin="1" 
                                                     VerticalAlignment="Bottom"
                                                     SnapsToDevicePixels="True"/>
                                         </Grid>
                                         <ControlTemplate.Triggers>
                                             <Trigger Property="IsEnabled" Value="false">
                                                 <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                                             </Trigger>
                                             <Trigger Property="IsMouseOver" Value="true">
                                                 <Setter Property="BorderBrush"  TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                                                 <Setter Property="Background"   TargetName="border" Value="{StaticResource TextBox.MouseOver.Background}"/>
                                             </Trigger>
                                             <Trigger Property="IsKeyboardFocused" Value="true">
                                                 <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focused.Border}"/>
                                                 <Setter Property="Background"  TargetName="border" Value="{StaticResource TextBox.Focused.Background}"/>
                                                 <!--<Setter Property="BorderThickness" TargetName="border" Value="1, 1, 1, 2"/>-->
                                                 <Setter Property="CornerRadius" TargetName="border" Value="3, 3, 0, 0"/>
                                                 <Setter Property="Visibility" TargetName="focusBar" Value="Visible"/>
                                             </Trigger>
                                         </ControlTemplate.Triggers>
                                     </ControlTemplate>
                                </DatePickerTextBox.Template>
                            </DatePickerTextBox>

                            <Grid x:Name="PART_DisabledVisual"
                                  Grid.ColumnSpan="2"
                                  Grid.Column="0"
                                  IsHitTestVisible="False"
                                  Opacity="0"
                                  Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <Rectangle Grid.Column="0"
                                           Fill="#A5FFFFFF"
                                           RadiusY="1"
                                           Grid.Row="0"
                                           RadiusX="1"/>

                                <Rectangle Grid.Column="1"
                                           Fill="#A5FFFFFF"
                                           Height="18"
                                           Margin="3, 0, 3, 0"
                                           RadiusY="1"
                                           Grid.Row="0"
                                           RadiusX="1"
                                           Width="19"/>

                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=PART_TextBox}"
                                       StaysOpen="False"/>
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>