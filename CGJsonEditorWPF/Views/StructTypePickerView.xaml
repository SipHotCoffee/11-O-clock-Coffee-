<editor:CustomWindow x:Class="CG.Test.Editor.Views.StructTypePickerView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:local="clr-namespace:CG.Test.Editor.Views" 
                     xmlns:viewmodels="clr-namespace:CG.Test.Editor.ViewModels"
                     xmlns:editor="clr-namespace:CG.Test.Editor"
                     mc:Ignorable="d"
                     Title="Select Structure" IsMicaBackgroundEnabled="False" WindowStartupLocation="CenterScreen" Height="300" Width="400">
    <Window.DataContext>
        <viewmodels:StructTypePickerViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding SearchText}" Margin="5, 5, 5, 2.5"/>
        <ListView Margin="5, 2.5, 5, 2.5" Grid.Row="1" ItemsSource="{Binding VisibleStructTypes}"/>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Margin="5, 2.5, 2.5, 5" Content="OK"/>
            <Button Grid.Column="1" Margin="2.5, 2.5, 5, 5" Content="Cancel" IsCancel="True"/>
        </Grid>
    </Grid>
</editor:CustomWindow>
