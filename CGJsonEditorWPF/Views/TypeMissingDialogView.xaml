<editor:CustomWindow xmlns:editor="clr-namespace:CG.Test.Editor"
                     x:Class="CG.Test.Editor.Views.TypeMissingDialogView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:local="clr-namespace:CG.Test.Editor.Views" 
                     xmlns:viewmodels="clr-namespace:CG.Test.Editor.ViewModels"
                     mc:Ignorable="d"
                     Title="{Binding TypeName}" WindowStartupLocation="CenterScreen" 
                     Height="450" 
                     Width="600" 
                     MinimizeButtonVisibility="Collapsed" MaximizeButtonVisibility="Collapsed" IsCloseButtonEnabled="False">
    <Window.DataContext>
        <viewmodels:TypeMissingDialogViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select Type:" Margin="5"/>

        <local:InstanceTypePickerControlView Grid.Row="1" DataContext="{Binding TypePicker}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Row="2" Margin="5, 2.5, 2.5, 5" Content="Next" IsDefault="True"
                    Command="{Binding NextCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"/>

            <Button Grid.Column="1" Margin="2.5, 2.5, 5, 5" Content="Discard" IsCancel="True"/> 
        </Grid>
        
    </Grid>
</editor:CustomWindow>
