<UserControl x:Class="CG.Test.Editor.Views.InstanceTypePickerControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CG.Test.Editor.Views" 
             xmlns:modelstypes="clr-namespace:CG.Test.Editor.Models.Types"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors" 
             xmlns:viewmodels="clr-namespace:CG.Test.Editor.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!--<TreeView x:Name="_treeView" Margin="5, 5, 2.5, 5" Grid.Column="0" ItemsSource="{Binding RootNodes}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectedItemChanged">
                    <i:InvokeCommandAction CommandParameter="{Binding ElementName=_treeView, Path=SelectedItem}" Command="{Binding SelectFolderCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <TreeView.Resources>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="IsExpanded" Value="True"/>
                </Style>
                
                <HierarchicalDataTemplate DataType="{x:Type viewmodels:EditorFolderTreeNode}" ItemsSource="{Binding Children}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Margin="4" Text="&#xE838;" FontSize="20" FontFamily="{StaticResource SymbolFont}"/>
                        <TextBlock Grid.Column="1" Margin="4" Text="{Binding Name}"/>
                    </Grid>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>

        <ListView x:Name="_listView" Grid.Column="1" Margin="2.5, 5, 5, 5" ItemsSource="{Binding Types}" SelectedItem="{Binding SelectedType}" SelectionMode="Single">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction CommandParameter="{Binding ElementName=_listView, Path=SelectedItem}" Command="{Binding SelectTypeCommand}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type modelstypes:EditorInstanceType}" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Margin="4" Text="&#xE8A5;" FontSize="20" FontFamily="{StaticResource SymbolFont}"/>
                        <TextBlock Grid.Column="1" Margin="4" Text="{Binding Name}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </Grid>
</UserControl>
