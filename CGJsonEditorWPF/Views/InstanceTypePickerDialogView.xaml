<editor:CustomWindow xmlns:editor="clr-namespace:CG.Test.Editor"
                     x:Class="CG.Test.Editor.Views.InstanceTypePickerDialogView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:local="clr-namespace:CG.Test.Editor.Views"
                     xmlns:viewmodels="clr-namespace:CG.Test.Editor.ViewModels" xmlns:system="clr-namespace:System;assembly=netstandard"
                     mc:Ignorable="d"
                     Title="InstanceTypePickerDialogView" 
                     Topmost="False" 
                     ShowInTaskbar="False" 
                     WindowStartupLocation="CenterScreen"
                     Height="500" 
                     Width="700">
    <editor:CustomWindow.Resources>
        <editor:IsNullConverter x:Key="IsNullConverter"/>
        <system:Boolean x:Key="NullConverterFlip">False</system:Boolean>
    </editor:CustomWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <local:InstanceTypePickerControlView DataContext="{Binding PickerViewModel}"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="OK"     Grid.Column="0" Margin="5, 5, 2.5, 5" IsDefault="True" IsEnabled="{Binding PickerViewModel.SelectedType, Converter={StaticResource IsNullConverter}, ConverterParameter={StaticResource NullConverterFlip}}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}" Command="{Binding OkCommand}"/>
            <Button Content="Cancel" Grid.Column="1" Margin="2.5, 5, 5, 5" IsCancel="True"/>
        </Grid>
    </Grid>
</editor:CustomWindow>
