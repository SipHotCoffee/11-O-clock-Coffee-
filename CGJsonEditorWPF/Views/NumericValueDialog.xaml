<editor:CustomWindow x:Class="CG.Test.Editor.Json.Dialogs.NumericValueDialog"
                     xmlns:editor="clr-namespace:CG.Test.Editor"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:views="clr-namespace:CG.Test.Editor.Views"
                     mc:Ignorable="d" Height="auto" Width="300" SizeToContent="Height" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Topmost="False" ShowInTaskbar="False">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="_label" Text="Select Numeric Value:" Margin="4, 4, 2, 4" Padding="8"/>
        <views:SpinButton x:Name="_spinButton" Grid.Column="1" Margin="2, 4, 4, 4"/>

        <Grid Grid.Row="1" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="_okButton"     Grid.Column="0" Content="OK"     Padding="8" Margin="6, 6, 3, 6" IsDefault="True" Click="OkButton_Click"/>
            <Button x:Name="_cancelButton" Grid.Column="1" Content="Cancel" Padding="8" Margin="3, 6, 6, 6" IsCancel="True"/>
        </Grid>
    </Grid>
</editor:CustomWindow>
