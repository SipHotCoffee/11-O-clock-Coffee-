<editor:CustomWindow x:Class="CG.Test.Editor.Views.AcceptedNamesDialog"
                     xmlns:editor="clr-namespace:CG.Test.Editor"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:local="clr-namespace:CG.Test.Editor.Views" 
                     xmlns:viewmodels="clr-namespace:CG.Test.Editor.ViewModels" xmlns:system="clr-namespace:System;assembly=netstandard"
                     mc:Ignorable="d"
                     Title="" Height="300" Width="300" WindowStartupLocation="CenterScreen" ShowInTaskbar="False" Topmost="False">
    <Window.DataContext>
        <viewmodels:AcceptedNamesDialogViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <editor:HasAnyItemsConverter x:Key="HasAnyItemsConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ToolBar>
            <Button FontFamily="{StaticResource SymbolFont}" FontSize="20" Content="&#xE710;" IsEnabled="True" Command="{Binding InsertNameCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}"/>

            <Button x:Name="_deleteButton" 
                    FontFamily="{StaticResource SymbolFont}" 
                    FontSize="20"
                    Content="&#xE74D;" 
                    CommandParameter="{Binding ElementName=_nameListView, Path=SelectedItems}"
                    Command="{Binding DeleteNamesCommand}">
                <Button.IsEnabled>
                    <Binding ElementName="_nameListView" Path="SelectedItems.Count" Converter="{StaticResource HasAnyItemsConverter}">
                        <Binding.ConverterParameter>
                            <system:Boolean>False</system:Boolean>
                        </Binding.ConverterParameter>
                    </Binding>
                </Button.IsEnabled>
            </Button>
        </ToolBar>

        <ListView x:Name="_nameListView" Grid.Row="1" ItemsSource="{Binding AcceptedNames}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="OK"     Margin="6, 6, 3, 6" IsDefault="True" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}}" Command="{Binding OkCommand}"/>
            <Button Grid.Column="1" Content="Cancel" Margin="3, 6, 6, 6" IsCancel="True"/>
        </Grid>
    </Grid>
</editor:CustomWindow>
