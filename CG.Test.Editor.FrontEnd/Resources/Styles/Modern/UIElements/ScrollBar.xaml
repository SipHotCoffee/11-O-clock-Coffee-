<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="ScrollThumb.Normal.Background" Color="{DynamicResource Control2}"/>
    <SolidColorBrush x:Key="ScrollThumb.Normal.Border"     Color="{DynamicResource Control4}"/>

    <SolidColorBrush x:Key="ScrollThumb.MouseOver.Background" Color="{DynamicResource Control3}"/>
    <SolidColorBrush x:Key="ScrollThumb.MouseOver.Border"     Color="{DynamicResource Control6}"/>

    <Style x:Key="ScrollBarThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Background"  Value="{DynamicResource ScrollThumb.Normal.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ScrollThumb.Normal.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Margin="2"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"  Value="{DynamicResource ScrollThumb.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource ScrollThumb.MouseOver.Border}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="ScrollBarButton.Normal.Background" Color="{DynamicResource Control2}"/>
    <LinearGradientBrush x:Key="ScrollBarButton.Normal.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Control5}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Control3}" Offset="0.1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="ScrollBarButton.MouseOver.Background" Color="{DynamicResource Control3}"/>
    <LinearGradientBrush x:Key="ScrollBarButton.MouseOver.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Control7}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Control4}" Offset="0.1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="ScrollBarButton.Pressed.Background" Color="{DynamicResource Control1}"/>
    <SolidColorBrush x:Key="ScrollBarButton.Pressed.Border"     Color="{DynamicResource Control2}"/>

    <SolidColorBrush x:Key="ScrollBarButton.Disabled.Background" Color="{DynamicResource Control3}"/>
    <SolidColorBrush x:Key="ScrollBarButton.Disabled.Border"     Color="{DynamicResource Control3}"/>

    <Style x:Key="ScrollBarRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="Background"      Value="{DynamicResource ScrollBarButton.Normal.Background}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource ScrollBarButton.Normal.Border}"/>
        <Setter Property="Foreground"      Value="{DynamicResource Window.Active.Foreground}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <!--<Setter Property="FontSize"        Value="14"/>-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}"
                         BorderBrush="{TemplateBinding BorderBrush}"
                         BorderThickness="{TemplateBinding BorderThickness}"
                         Margin="2"
                         CornerRadius="4" 
                         SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="_contentPresenter"
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"
                                     Content="{TemplateBinding Content}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"  Value="{DynamicResource ScrollBarButton.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarButton.MouseOver.Border}"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"  Value="{DynamicResource ScrollBarButton.Pressed.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarButton.Pressed.Border}"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background"  Value="{DynamicResource ScrollBarButton.Disabled.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource ScrollBarButton.Disabled.Border}"/>
                            <Setter Property="Foreground"  Value="{DynamicResource Window.Active.Foreground.Disabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="0.00001*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <RepeatButton x:Name="PART_LineLeftButton" Grid.Column="0" Command="{x:Static ScrollBar.LineLeftCommand}" Width="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ActualHeight}" FontFamily="{DynamicResource SymbolFont}" FontSize="12" Content="&#xE76B;" Style="{DynamicResource ScrollBarRepeatButtonStyle}"/>
            <Track x:Name="PART_Track" Grid.Column="1" Orientation="Horizontal" IsDirectionReversed="False">
                <Track.Thumb>
                    <Thumb Style="{DynamicResource ScrollBarThumbStyle}"/>
                </Track.Thumb>
            </Track>
            <RepeatButton x:Name="PART_LineRightButton" Grid.Column="2" Command="{x:Static ScrollBar.LineRightCommand}" Width="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ActualHeight}" FontFamily="{DynamicResource SymbolFont}" FontSize="12" Content="&#xE76C;" Style="{DynamicResource ScrollBarRepeatButtonStyle}"/>
        </Grid>
    </ControlTemplate>

    <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="0.00001*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <RepeatButton x:Name="PART_LineUpButton" Grid.Row="0" Command="{x:Static ScrollBar.LineUpCommand}" FontFamily="{DynamicResource SymbolFont}" FontSize="12" Content="&#xE70E;" Height="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ActualWidth}" Style="{DynamicResource ScrollBarRepeatButtonStyle}"/>
            <Track x:Name="PART_Track" Grid.Row="1" Orientation="Vertical" IsDirectionReversed="True">
                <Track.Thumb>
                    <Thumb Style="{DynamicResource ScrollBarThumbStyle}"/>
                </Track.Thumb>
            </Track>
            <RepeatButton x:Name="PART_LineDownButton" Grid.Row="2" Command="{x:Static ScrollBar.LineDownCommand}" FontFamily="{DynamicResource SymbolFont}" FontSize="12" Content="&#xE70D;" Height="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ActualWidth}" Style="{DynamicResource ScrollBarRepeatButtonStyle}"/>
        </Grid>
    </ControlTemplate>


    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Template"  Value="{DynamicResource VerticalScrollBarTemplate}"/>
        <Setter Property="MinWidth"  Value="25"/>
        <Setter Property="MinHeight" Value="25"/>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{DynamicResource HorizontalScrollBarTemplate}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>