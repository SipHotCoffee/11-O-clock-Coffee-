<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
        <ResourceDictionary Source="Items.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="Menu.Separator.Background" Color="{StaticResource Control4}"/>

    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Menu}">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="{x:Static MenuItem.SeparatorStyleKey}" TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{StaticResource Menu.Separator.Background}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Rectangle Fill="{TemplateBinding Background}" Height="1" Margin="8, 0, 8, 0"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="TopLevelMenuItem" TargetType="{x:Type MenuItem}">
        <Border Background ="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="1"
                Margin="3"
                Padding="8"
                CornerRadius="4">
            <Grid>
                <ContentPresenter Content="{TemplateBinding Header}"/>

                <Popup x:Name="_popup" 
                       Grid.ColumnSpan="2"
                       IsOpen="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsSubmenuOpen, Mode=OneWay}"
                       AllowsTransparency="True"
                       Placement="Bottom"
                       PlacementTarget="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}">
                    <Border Background="{StaticResource Window.Active.Background}" BorderThickness="1" CornerRadius="4">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="White" Opacity="0.1"/>
                        </Border.BorderBrush>
                        <ItemsPresenter/>
                    </Border>

                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background"  Value="{StaticResource Item.MouseOver.Background}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Item.MouseOver.Border}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background"  Value="{StaticResource Item.Selected.Background}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Item.Selected.Border}"/>
            </Trigger>
			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Foreground" Value="{StaticResource Window.Active.Foreground.Disabled}"/>
			</Trigger>
			<Trigger Property="IsSubmenuOpen" Value="True">
                <Setter Property="Background"  Value="{StaticResource Item.Selected.Background}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Item.Selected.Border}"/>
                
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SubMenuItem" TargetType="{x:Type MenuItem}">
        <Border Background ="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="1"
                Margin="3"
                Padding="8"
                CornerRadius="4">
            <Grid MinWidth="100">
                <ContentPresenter Content="{TemplateBinding Header}"/>

                <Popup x:Name="_popup" 
                       Grid.ColumnSpan="2"
                       IsOpen="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=IsSubmenuOpen, Mode=OneWay}"
                       AllowsTransparency="True"
                       Placement="Bottom"
                       PlacementTarget="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}">
                    <Border Background="{StaticResource Window.Active.Background}" BorderThickness="1" CornerRadius="4">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="White" Opacity="0.1"/>
                        </Border.BorderBrush>
                        <ItemsPresenter/>
                    </Border>

                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background"  Value="{StaticResource Item.MouseOver.Background}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Item.MouseOver.Border}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background"  Value="{StaticResource Item.Selected.Background}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Item.Selected.Border}"/>
            </Trigger>
            <Trigger Property="IsSubmenuOpen" Value="True">
                <Setter Property="Background"  Value="{StaticResource Item.Selected.Background}"/>
				<Setter Property="BorderBrush" Value="{StaticResource Item.Selected.Border}"/>
			</Trigger>
			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Foreground" Value="{StaticResource Window.Active.Foreground.Disabled}"/>
			</Trigger>
		</ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type MenuItem}">
        
        <Setter Property="Foreground" Value="{StaticResource Window.Active.Foreground}"/>
        <Setter Property="FontSize" Value="14"/>
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Template" Value="{StaticResource TopLevelMenuItem}"/>
            </Trigger>
            <Trigger Property="Role" Value="TopLevelItem">
                <Setter Property="Template" Value="{StaticResource TopLevelMenuItem}"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource SubMenuItem}"/>
            </Trigger>
			<Trigger Property="Role" Value="SubmenuItem">
				<Setter Property="Template" Value="{StaticResource SubMenuItem}"/>
			</Trigger>
		</Style.Triggers>
    </Style>
</ResourceDictionary>