<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="TabControl.Background" Color="{DynamicResource Control2}"/>
    <SolidColorBrush x:Key="TabControl.Border"     Color="{DynamicResource Control3}"/>

    <SolidColorBrush x:Key="TabHeader.Normal.Background" Color="{DynamicResource Control2}"/>
    <LinearGradientBrush x:Key="TabHeader.Normal.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Control5}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Control3}" Offset="0.1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="TabHeader.MouseOver.Background" Color="{DynamicResource Control3}"/>
    <LinearGradientBrush x:Key="TabHeader.MouseOver.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Control7}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Control4}" Offset="0.1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <Style TargetType="{x:Type TabItem}">
        <Setter Property="BorderBrush" Value="{DynamicResource TabHeader.Normal.Border}"/>
        <Setter Property="Background" Value="{DynamicResource TabHeader.Normal.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Window.Active.Foreground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="Border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="1"
                            CornerRadius="6"
                            Margin="4, 4, 0, 4"
                            MinWidth="130"
                            MinHeight="35">
                        <ContentPresenter x:Name="ContentSite" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Stretch" ContentSource="Header"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="CornerRadius"    Value="6, 6, 0, 0"/>
                            <Setter TargetName="Border" Property="Margin"          Value="4, 4, 0, 0"/>
                            <Setter TargetName="Border" Property="BorderThickness" Value="1, 1, 1, 0"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected"  Value="False"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>

                            <Setter Property="Background"  Value="{DynamicResource TabHeader.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource TabHeader.MouseOver.Border}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
    </Style>

    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Padding"                    Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Background"                 Value="{DynamicResource TabHeader.Normal.Background}"/>
        <Setter Property="BorderBrush"                Value="{DynamicResource TabHeader.Normal.Border}"/>
        <Setter Property="BorderThickness"            Value="1, 0, 1, 1"/>
        <Setter Property="Foreground"                 Value="{DynamicResource Window.Active.Foreground}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TabPanel x:Name="HeaderPanel" Margin="4, 0, 0, 0" Grid.Row="0" IsItemsHost="True"/>
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" Grid.Row="1">
                            <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    

</ResourceDictionary>