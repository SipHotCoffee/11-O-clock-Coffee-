<frontend:CustomWindow x:Class="CG.Test.Editor.FrontEnd.Views.Dialogs.StringValueDialog"
					   xmlns:frontend="clr-namespace:CG.Test.Editor.FrontEnd"
					   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					   xmlns:local="clr-namespace:CG.Test.Editor.FrontEnd.Views.Dialogs"
					   mc:Ignorable="d"
					   Height="auto" 
					   Width="400" 
					   SizeToContent="Height" 
					   WindowStartupLocation="CenterScreen" 
					   ResizeMode="NoResize" Topmost="False" ShowInTaskbar="False" 
					   Title="Enter Text"
					   MinimizeButtonVisibility="Collapsed"
					   MaximizeButtonVisibility="Collapsed" 
					   DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>

		<TextBox Margin="5, 5, 5, 2.5"
				 MaxLength="{Binding MaximumLength}"
				 Text="{Binding Text}"/>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<Button x:Name="_okButton"     Grid.Column="0" Content="OK"     Padding="8" Margin="5, 5, 2.5, 5" IsDefault="True" Click="OkButton_Click"/>
			<Button x:Name="_cancelButton" Grid.Column="1" Content="Cancel" Padding="8" Margin="2.5, 5, 5, 5" IsCancel="True"/>
		</Grid>
	</Grid>
</frontend:CustomWindow>
