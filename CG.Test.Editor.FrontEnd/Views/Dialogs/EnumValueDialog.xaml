<frontend:CustomWindow x:Class="CG.Test.Editor.FrontEnd.Views.Dialogs.EnumValueDialog"
					   xmlns:frontend="clr-namespace:CG.Test.Editor.FrontEnd"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:local="clr-namespace:CG.Test.Editor.FrontEnd.Views.Dialogs"
                       mc:Ignorable="d"
                       Title="Select enum value" Height="150" Width="300" 
					   WindowStartupLocation="CenterScreen" 
					   DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
					   MinimizeButtonVisibility="Collapsed">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>

		<ComboBox Grid.Row="0" Margin="5" 
				  ItemsSource="{Binding PossibleValues}" 
				  SelectedIndex="{Binding SelectedIndex}" 
				  SelectedItem="{Binding SelectedItem}">
			<ComboBox.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding}"/>
				</DataTemplate>
			</ComboBox.ItemTemplate>
		</ComboBox>

		<Grid Grid.Row="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>

			<Button Grid.Column="0" Margin="5, 5, 2.5, 5" Content="OK" IsDefault="True" Click="OkButton_Click"/>
			<Button Grid.Column="1" Margin="2.5, 5, 5, 5" Content="Cancel" IsCancel="True"/>
		</Grid>
    </Grid>
</frontend:CustomWindow>
