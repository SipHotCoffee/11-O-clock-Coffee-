<frontend:CustomWindow x:Class="CG.Test.Editor.FrontEnd.Views.Dialogs.VariantTypeSelectorDialog"
					   xmlns:frontend="clr-namespace:CG.Test.Editor.FrontEnd"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:local="clr-namespace:CG.Test.Editor.FrontEnd.Views.Dialogs"
                       mc:Ignorable="d"
                       Title="Select variant type" Height="400" Width="600" WindowStartupLocation="CenterScreen" DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="auto"/>
			</Grid.ColumnDefinitions>
			<ListView Grid.Column="0" Margin="5, 5, 2.5, 2.5" ItemsSource="{Binding PossibleTypes}" SelectedIndex="0" SelectedItem="{Binding SelectedType}">
				<ListView.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding Name}"/>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>

			<ListView Grid.Column="1" Margin="2.5, 5, 5, 2.5" ItemsSource="{Binding SelectedType.Properties}">
				<ListView.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding Name}"/>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</Grid>
			
		<Button Grid.Row="1" HorizontalAlignment="Right" Width="150" Content="OK" Margin="5, 2.5, 5, 5" Click="Ok_Button_Click"/>
	</Grid>
</frontend:CustomWindow>
