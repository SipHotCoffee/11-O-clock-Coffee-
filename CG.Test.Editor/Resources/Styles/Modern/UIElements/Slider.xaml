<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="Slider.Track.Background" Color="{StaticResource Control2}"/>

    <LinearGradientBrush x:Key="Slider.Track.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{StaticResource Control2}" Offset="0.95"/>
            <GradientStop Color="{StaticResource Control5}" Offset="1.0"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Slider.ThumbCentre.Background" Color="{StaticResource Accent4}"/>

    <SolidColorBrush x:Key="Slider.Thumb.Border" Color="{StaticResource Control4}"/>

    <Style TargetType="Slider">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <Border Height="5" CornerRadius="2.5" BorderThickness="1" Background="Transparent" BorderBrush="{StaticResource Slider.Track.Border}"/>

                        <Track Name="PART_Track">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Style>
                                        <Style TargetType="{x:Type Thumb}">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type Thumb}">
                                                        <Grid>
                                                            <Ellipse HorizontalAlignment="Center" VerticalAlignment="Center" Stroke="{StaticResource Slider.Thumb.Border}" StrokeThickness="1" Fill="{StaticResource Window.Active.Background}" Width="25" Height="25"/>
                                                            <Ellipse x:Name="CentreEllipse" HorizontalAlignment="Center" VerticalAlignment="Center"  Fill="{StaticResource Slider.ThumbCentre.Background}" Width="12" Height="12"/>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Trigger.Setters>
                                                                    <Setter TargetName="CentreEllipse" Property="Width"  Value="15"/>
                                                                    <Setter TargetName="CentreEllipse" Property="Height" Value="15"/>
                                                                </Trigger.Setters>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Thumb.Style>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>