<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Window.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <SolidColorBrush x:Key="Button.Normal.Background" Color="{DynamicResource Control2}"/>
    <LinearGradientBrush x:Key="Button.Normal.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Control5}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Control3}" Offset="0.1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Button.MouseOver.Background" Color="{DynamicResource Control3}"/>
    <LinearGradientBrush x:Key="Button.MouseOver.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Control7}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Control4}" Offset="0.1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Button.Pressed.Background" Color="{DynamicResource Control1}"/>
    <SolidColorBrush x:Key="Button.Pressed.Border"     Color="{DynamicResource Control2}"/>

    <SolidColorBrush x:Key="Button.Disabled.Background" Color="{DynamicResource Control3}"/>
    <SolidColorBrush x:Key="Button.Disabled.Border"     Color="{DynamicResource Control3}"/>

    <SolidColorBrush x:Key="Button.Defaulted.Normal.Background" Color="{DynamicResource Accent4}"/>
    <LinearGradientBrush x:Key="Button.Defaulted.Normal.Border" StartPoint="0, 0" EndPoint="0, 1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="{DynamicResource Accent5}" Offset="0.05"/>
            <GradientStop Color="{DynamicResource Accent4}" Offset="0.1"/>
            <GradientStop Color="{DynamicResource Accent4}" Offset="0.95"/>
            <GradientStop Color="{DynamicResource Accent2}" Offset="1.0"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <Style TargetType="{x:Type RepeatButton}">
        <Setter Property="Background"      Value="{DynamicResource Button.Normal.Background}"/>
        <Setter Property="BorderBrush"     Value="{DynamicResource Button.Normal.Border}"/>
        <Setter Property="Foreground"      Value="{DynamicResource Window.Active.Foreground}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" 
                            Padding="8"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="_contentPresenter"
                                        HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding Content}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background"  Value="{DynamicResource Button.MouseOver.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource Button.MouseOver.Border}"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background"  Value="{DynamicResource Button.Pressed.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource Button.Pressed.Border}"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background"  Value="{DynamicResource Button.Disabled.Background}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource Button.Disabled.Border}"/>
                            <Setter Property="Foreground"  Value="{DynamicResource Window.Active.Foreground.Disabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>